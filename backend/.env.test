# Test Environment Configuration

# Database Configuration (Test Database)
DATABASE_URL="postgresql://username:password@localhost:5432/test_database_name"

# Supabase Test Configuration (Optional - use separate test project)
SUPABASE_URL="https://your-test-project-id.supabase.co"
SUPABASE_ANON_KEY="your-test-supabase-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-test-supabase-service-role-key"

# JWT Configuration (Test)
JWT_SECRET="test-jwt-secret-key-for-testing-only"
JWT_EXPIRES_IN="1h"

# Server Configuration
NODE_ENV="test"
PORT=5002

# CORS Configuration
FRONTEND_URL="http://localhost:3000"

# Logging
LOG_LEVEL="error"